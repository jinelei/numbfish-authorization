server:
  port: 9999
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.jinelei.iotgenius.auth.client.controller

# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn
  # 开启Swagger的Basic认证功能,默认是false
  basic:
    enable: true
    # Basic认证用户名
    username: iotgenius
    # Basic认证密码
    password: iotgenius

# Logger Config
logging:
  level:
    root: warn
    com.jinelei: debug

spring:
  application:
    name: iotgenius-auth-service
  banner:
    location: banner.txt
  datasource:
    url: jdbc:mysql://192.168.3.130:3306/iotgenius?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: iotG@2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.3.130
      port: 6379
      password: iotG@2025
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-wait: -1
          max-idle: 8
          min-idle: 0

mybatis:
  mapper-locations: classpath*:mapper/*/*Mapper.xml,classpath*:mapper/*Mapper.xml
  type-handlers-package: com.jinelei.iotgenius.auth.client.configuration.handler.type
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
iotgenius:
  auth:
    admin:
      username: administrator
    ignore:
      urls: 
        - /webjars
        - /doc.html
        - /user/login
